# Rama
-Import the Database inside SqlServer (Slack)

-Add new user to the Database “as“ and the password “as“ and give it db owner permission. 

-From the command line at e root folder, run “ npm i ” after the installing run “npm run prod“ then  “npm run build:w“

By running the application from the VS, you will get a new window/browser you need to provide User name, Email and the Password. Also in the right hand you need to choose Minimum in the slider. 

Then press install. 

-By adding /umbraco to the url you can enter the Bckoffice. 
